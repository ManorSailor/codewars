// Best practice | First attempt
function arrowArea(a, b) {
    // Area of Triangle: 1/2 * a * b.
    // Divide the recatangle into two triangles, divide those two trianlges into 2 triangles
    return (((a * b) / 2) / 2);
}